#ifndef __PLATFORM_H_
#define __PLATFORM_H_

/* Initializes the platform (e.g., caches, etc.) */
void init_platform(void);

/* Cleans up the platform before exiting */
void cleanup_platform(void);

#endif /* __PLATFORM_H_ */
